name: MyWorkFlow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-slim

    steps:
      - name: first-stage
        run: echo "welcome to First Github Pipeline"

      - name: second-stage
        run: echo "this is my second stage"

      - name: stage-3
        run: echo This is stage-3
